jdk:
  - openjdk17
install:
  - ./gradlew clean build -xtest
  - ./gradlew publishToMavenLocal
  - ./gradlew :shell:publish
  - ./gradlew :tools:publish
  - ls tools/build/libs
  - ls shell/build/libs
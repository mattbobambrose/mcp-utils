jdk:
  - openjdk17
install:
  - ./gradlew clean build -xtest
  - ./gradlew :shell:publishToMavenLocal
  - ./gradlew :tools:publishToMavenLocal
  #  - ./gradlew :shell:publish
  #  - ./gradlew :tools:publish
  - ls tools/build/libs
  - ls shell/build/libs